name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rust:
    name: Rust checks (${{ matrix.label }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-15
            label: supported-macos-arm64
            target: aarch64-apple-darwin
            architecture: arm64
          - os: ubuntu-latest
            label: configured-release-target-linux-x86_64-pending-hosted-evidence
            target: x86_64-unknown-linux-gnu
            architecture: x86_64
    runs-on: ${{ matrix.os }}
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Install pinned Rust toolchain
        run: rustup show active-toolchain
      - name: Fetch locked dependencies
        run: cargo fetch --locked
      - name: Verify native runner architecture
        run: test "$(uname -m)" = "${{ matrix.architecture }}"
      - name: Reject legacy implementation files
        shell: bash
        run: |
          test -z "$(git ls-files '*.py')"
          for path in pyproject.toml uv.lock .python-version pyrightconfig.json MANIFEST.in; do
            test -z "$(git ls-files "$path")"
          done
      - name: Run workspace gates
        run: cargo xtask check
      - name: Verify desktop transport fixture
        run: node --test scripts/check-desktop-transport.cjs
      - name: Build and verify sealed native release
        shell: bash
        run: |
          version="$(sed -n 's/^version = "\([^"]*\)"/\1/p' Cargo.toml | head -1)"
          cargo xtask release build-native \
            --target "${{ matrix.target }}" \
            --output "$RUNNER_TEMP/native" \
            --version "$version"
          cargo xtask release verify --release "$RUNNER_TEMP/native/releases/$version"

  release-contract:
    name: Rust release contract
    runs-on: macos-15
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Install pinned Rust toolchain
        run: rustup show active-toolchain
      - name: Fetch locked dependencies
        run: cargo fetch --locked
      - name: Verify release evidence and source archive
        run: |
          cargo xtask qualify --release
          cargo xtask evidence verify
          cargo xtask archive --verify
      - name: Build and verify sealed native release
        shell: bash
        run: |
          version="$(sed -n 's/^version = "\([^"]*\)"/\1/p' Cargo.toml | head -1)"
          cargo xtask release build-native --output "$RUNNER_TEMP/native" --version "$version"
          cargo xtask release verify --release "$RUNNER_TEMP/native/releases/$version"
