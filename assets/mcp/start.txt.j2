agent-run start accepted (asynchronous durable run)

- ID: {{ agent_id }} — durable; this response is NOT a completion
- Admission: {{ "created" if created else "replayed" }}
- Status: {{ status }}{% if phase %} ({{ phase }}){% endif %}{{ "" }}
- Route: {{ runtime }}/{{ model }} profile {{ profile }}
{% if effort %}
- Effort: {{ effort }}
{% endif %}
{% if attempt_id %}
- Attempt: {{ attempt_id }}
{% endif %}
{% if parent_agent_id %}
- Parent: {{ parent_agent_id }} (native-context continuation)
{% endif %}
- Notice binding: {{ "bound" if bound else "not bound" }}; use list_agents, transcript, or answer with the ID above for progress and results
